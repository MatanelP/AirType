# OS
.DS_Store
Thumbs.db

# Node
node_modules
/build
/.svelte-kit
/package

# Environment
.env
.env.*
!.env.example

# Vite
vite.config.js.timestamp-*
vite.config.ts.timestamp-*

# Rust/Cargo
src-tauri/target/
# Cargo.lock IS committed: AirType ships as an application, so we pin exact
# dependency versions to keep CI reproducible (a fresh resolve once pulled a
# broken time-macros that failed the macOS build).
**/*.rs.bk

# IDE
.idea/
.vscode/*
!.vscode/extensions.json
*.swp
*.swo

# Logs
*.log
npm-debug.log*

# Models (too large for git)
*.bin
*.ggml

# Build artifacts
*.exe
*.dll
*.dylib
*.so

# Tauri
src-tauri/gen/

# Context docs (for development)
AGENT_CONTEXT.md
PLAN.md
PROPOSAL.md
